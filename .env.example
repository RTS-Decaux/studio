# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on Vercel

# Optional: pick which provider powers default chat models (openai | gemini)
AI_DEFAULT_PROVIDER=openai

# OpenAI configuration
OPENAI_API_KEY=****

# Google Gemini configuration
GEMINI_API_KEY=****


# Instructions to create a PostgreSQL database here: https://vercel.com/docs/postgres
# (Deprecated after migrating to Supabase; prefer SUPABASE_* variables below)
POSTGRES_URL=****

# Supabase project configuration
NEXT_PUBLIC_SUPABASE_URL=****
NEXT_PUBLIC_SUPABASE_ANON_KEY=****
# Keep service role secrets server-side only (never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=****
SUPABASE_DB_URL=****
# Optional: provide aliases for tooling that expects SUPABASE_URL/ANON_KEY
SUPABASE_URL=****
SUPABASE_ANON_KEY=****

# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****
