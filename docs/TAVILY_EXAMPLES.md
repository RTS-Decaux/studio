# Примеры использования Tavily Search

## Быстрый старт

### 1. Простой поиск

**Пользователь:**
```
Найди последние новости о Next.js 15
```

**Модель использует:**
```typescript
webSearch({
  query: "Next.js 15 latest news",
  maxResults: 5
})
```

**Результат:**
Модель получает актуальные статьи и новости о Next.js 15 и формирует структурированный ответ.

---

### 2. Глубокий поиск с большим количеством результатов

**Пользователь:**
```
Мне нужна подробная информация о новых возможностях React 19
```

**Модель использует:**
```typescript
webSearch({
  query: "React 19 new features detailed",
  maxResults: 10,
  searchDepth: "advanced"
})
```

---

### 3. Поиск без сгенерированного ответа

**Пользователь:**
```
Покажи мне источники информации о TypeScript 5.5
```

**Модель использует:**
```typescript
webSearch({
  query: "TypeScript 5.5 documentation",
  maxResults: 5,
  includeAnswer: false
})
```

---

## Практические сценарии

### Сценарий 1: Исследование технологии

**Задача:** Узнать о новой библиотеке

```
Расскажи мне о библиотеке Shadcn UI. Что это, как использовать?
```

**Что происходит:**
1. Модель определяет, что нужна актуальная информация
2. Вызывает `webSearch` с запросом "Shadcn UI overview tutorial"
3. Получает результаты с официальных источников
4. Формирует структурированный ответ с примерами

---

### Сценарий 2: Сравнение решений

**Задача:** Сравнить два фреймворка

```
Сравни Vercel AI SDK с LangChain для создания AI-приложений
```

**Что происходит:**
1. Первый поиск: "Vercel AI SDK features benefits"
2. Второй поиск: "LangChain features benefits"
3. Модель анализирует результаты и создает сравнительную таблицу

---

### Сценарий 3: Отладка и поиск решений

**Задача:** Найти решение проблемы

```
Как исправить ошибку "Module not found: Can't resolve 'ai'" в Next.js?
```

**Что происходит:**
1. Поиск: "Next.js Module not found ai package solution"
2. Модель находит релевантные StackOverflow, GitHub Issues
3. Предлагает проверенные решения

---

### Сценарий 4: Актуальные новости и события

**Задача:** Узнать о текущих событиях

```
Что нового в мире AI за последнюю неделю?
```

**Что происходит:**
1. Поиск: "AI artificial intelligence latest news this week"
2. Получение свежих новостей с датами публикации
3. Краткая сводка основных событий

---

### Сценарий 5: Создание контента на основе веб-данных

**Задача:** Создать отчет с актуальными данными

```
Создай документ с обзором лучших практик Next.js App Router в 2025 году
```

**Что происходит:**
1. `webSearch`: "Next.js App Router best practices 2025"
2. Модель анализирует результаты
3. `createDocument`: Создается структурированный документ
4. Результат: Markdown документ с актуальной информацией

---

## Комбинирование инструментов

### Пример 1: Поиск + Погода

```
Найди информацию о предстоящих событиях в Лондоне и покажи погоду там
```

**Инструменты:**
1. `webSearch({ query: "upcoming events London" })`
2. `getWeather({ city: "London" })`

---

### Пример 2: Поиск + Создание документа

```
Изучи документацию Tavily API и создай гайд по интеграции
```

**Инструменты:**
1. `webSearch({ query: "Tavily API documentation integration guide" })`
2. `createDocument({ title: "Tavily Integration Guide", content: "..." })`

---

### Пример 3: Множественные поиски + Анализ

```
Сравни производительность различных AI SDK и создай таблицу
```

**Инструменты:**
1. `webSearch({ query: "Vercel AI SDK performance benchmarks" })`
2. `webSearch({ query: "LangChain performance benchmarks" })`
3. `webSearch({ query: "OpenAI SDK performance benchmarks" })`
4. Модель создает сравнительную таблицу в ответе

---

## Тестирование

### Тест 1: Базовый функционал

```bash
# В чате отправьте:
"Найди информацию о TypeScript 5.6"
```

**Ожидаемый результат:**
- Модель использует webSearch
- Возвращает актуальную информацию
- Включает ссылки на источники

---

### Тест 2: Обработка ошибок

```bash
# Временно удалите TAVILY_API_KEY из .env.local
# В чате отправьте:
"Найди последние новости"
```

**Ожидаемый результат:**
- Ошибка: "Web search is not configured"
- Приложение продолжает работать
- Модель может использовать знания из обучения

---

### Тест 3: Параметры поиска

```bash
# В чате отправьте:
"Найди топ-10 статей о React Server Components"
```

**Ожидаемый результат:**
- Модель автоматически использует maxResults: 10
- Возвращает больше результатов

---

## Отладка

### Проверка вызова инструмента

Включите логирование в `web-search.ts`:

```typescript
execute: async ({ query, maxResults = 5, searchDepth = "basic", includeAnswer = true }) => {
  console.log("🔍 Web Search:", { query, maxResults, searchDepth, includeAnswer });
  
  // ... rest of code
}
```

### Проверка результатов

Добавьте логирование результатов:

```typescript
const response = await tavilyClient.search(query, { ... });
console.log("📊 Search Results:", {
  query,
  resultsCount: response.results.length,
  hasAnswer: !!response.answer,
  responseTime: response.responseTime
});
```

---

## Лучшие практики

### 1. Формулирование запросов

✅ **Хорошо:**
- "Next.js 15 new features"
- "TypeScript 5.6 release notes"
- "React Server Components tutorial"

❌ **Плохо:**
- "что такое next" (слишком общо)
- "найди мне информацию" (нет конкретики)
- "aaaaaaa" (бессмысленный запрос)

### 2. Использование searchDepth

- `basic`: Для быстрых фактических запросов
- `advanced`: Для исследований и детального анализа

### 3. Количество результатов

- `3-5`: Оптимально для большинства запросов
- `7-10`: Для сравнительного анализа
- `1-2`: Для быстрой проверки факта

### 4. Когда использовать includeAnswer

- `true`: Когда нужен готовый ответ на вопрос
- `false`: Когда нужны только источники для дальнейшего анализа

---

## Расширенные примеры

### Пример: Мониторинг технологий

```typescript
// Создайте периодический поиск новостей
const topics = [
  "Next.js updates",
  "Vercel AI SDK news",
  "React latest features"
];

for (const topic of topics) {
  const results = await webSearch({
    query: topic,
    maxResults: 3,
    searchDepth: "basic"
  });
  
  // Обработка результатов
}
```

### Пример: Исследование конкурентов

```typescript
const competitors = [
  "OpenAI API",
  "Anthropic Claude API",
  "Google Gemini API"
];

const analyses = await Promise.all(
  competitors.map(competitor =>
    webSearch({
      query: `${competitor} features pricing`,
      maxResults: 5,
      searchDepth: "advanced"
    })
  )
);

// Создание сравнительного отчета
```

---

## Заключение

Tavily Search значительно расширяет возможности вашего AI-ассистента, позволяя:

- ✅ Получать актуальную информацию в реальном времени
- ✅ Проверять факты и события
- ✅ Находить технические решения
- ✅ Создавать контент на основе свежих данных
- ✅ Комбинировать с другими инструментами для сложных задач

Экспериментируйте с различными параметрами и сценариями использования!
